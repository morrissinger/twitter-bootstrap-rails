<%%= form_for @<%= resource_name %>, :html => { :class => 'form-horizontal' } do |f| %>
  <%- columns.each do |column| -%>
  <div class="form-group">
    <%%= f.label :<%= column.name %>, :class => 'col-md-2 control-label' %>
    <div class="col-md-10">
      <%%= f.<%= column.field_type %> :<%= column.name %>, :class => 'form-control <%= column.field_type %>' %>
    </div>
  </div>
  <%- end -%>

  <div class="form-actions">
    <%%= f.submit nil, :class => 'btn btn-primary' %>
    <%%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                <%= controller_routing_path %>_path, :class => 'btn' %>
  </div>
<%% end %>
